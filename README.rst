Rubin ATS documents the Rubin Automatic Testing System (ATS) for the Telescope Mount Assembly (TMA).

Build the documentation environment with ``uv sync --group docs``.
Build the HTML site with ``make html``.
Open ``_build/html/index.html`` to view the generated site.
Remove generated documentation files with ``make clean``.
